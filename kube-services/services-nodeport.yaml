apiVersion: v1
kind: Service
metadata:
  name: sagar-service
spec:
  type: NodePort
  selector:
    app: sagar-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080